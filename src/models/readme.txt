this is used to create the model that will model to the database.
we create the model and provide all the validations on it and then
export this obejct to main file.

in index file we simply create the obeject of this model and send to database

**** this is same as that of activities in MKCL where we were providing the need to create 
object and then simply use that object *****